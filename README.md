# Getting Started

```bash
# Step 1: Create and activate a new Python virtualenv
virtualenv -p python env
source env/bin/activate
# Step 2: Install dependencies
pip install -r requirements.txt
# Step 3: Copy `.env.example` to `.env` and add the proper variables
cp .env.example .env
```